# Local dev (Pelican sets SERVER_PORT automatically from the allocated port)
PORT=3000
HOST=0.0.0.0

CONTACT_TO=info@nuovacopertura.it

# Optional: SMTP (per invio email reale)
SMTP_HOST=smtp.zoho.eu
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=info@yrb4g.com
SMTP_PASS=YOUR_PASSWORD_HERE
SMTP_FROM="Nuova Copertura <info@yrb4g.com>"

